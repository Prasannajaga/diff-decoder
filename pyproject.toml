[project]
name = "diff-decoder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.10.*"
dependencies = [
    "accelerate>=1.11.0", 
    "flash-attn>=2.8.3",
    "rich>=14.3.3",
    "torch>=2.1.0",
]
 